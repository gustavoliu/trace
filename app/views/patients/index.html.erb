<div>cont</div>

<%= form_tag patients_path, method: :get do %>
  <%= text_field_tag 'term', params[:term], placeholder: "Paciente ou número do SUS" %>
  <%= submit_tag 'Buscar' %>
<% end %>

<ul>
  <% @patients.each do |patient| %>

    <li><%= link_to patient.full_name, patient_path(patient) %></li>

  <% end %>
</ul>
