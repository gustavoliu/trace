<div class="container">
  <%= simple_form_for( [@consult, @soap] ) do |f| %>
  <div class="row">
    <div class="col-xs-12 col-sm-6 ">
     <%= f.input :complaint, label: 'Queixa' %>
   </div>
   <div class="col-xs-12 col-sm-6 ">
     <%= f.input :diagnosis, label: 'Diagnóstico' %>
   </div>
   <div class="col-xs-12 col-sm-6 ">
     <%= f.input :exams, as: :check_boxes, collection: Soap.exams.keys, label: 'Exames' %>
    </div>
    <div class="col-xs-12 col-sm-6 ">
      <%= f.input :refering, as: :check_boxes, collection: Soap.referings.keys, label: 'Conduta' %>
      <%= f.submit 'Salvar', class: 'btn btn-primary' %>
    </div>
      <% end %>
  </div>
</div>





