<%= simple_form_for( [@consult, @soap] ) do |f| %>
  <%= f.input :complaint, label: 'Queixa' %>
  <%= f.input :diagnosis, label: 'Diagnóstico' %>
  <%= f.input :exams, collection: Soap.exams.keys, label: 'Exames' %>
  <%= f.input :refering, collection: Soap.referings.keys, label: 'Conduta' %>
  <%= f.button :submit, 'Salvar' %>
<% end %>
